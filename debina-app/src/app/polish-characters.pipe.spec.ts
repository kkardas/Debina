import { PolishCharactersPipe } from './polish-characters.pipe';

describe('PolishCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new PolishCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
